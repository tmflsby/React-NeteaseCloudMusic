import React, { memo } from "react";
import { WrapperContainer } from "./styled";

const HeaderComp = memo(() => {
  return <WrapperContainer>this is header</WrapperContainer>
})

export default HeaderComp
